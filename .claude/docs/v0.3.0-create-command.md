# v0.3.0 — create 命令

## 目标

实现 `pnpm run create` 命令，通过交互式选择从 `templates/` 复制模板到根目录，并自动安装依赖。

---

## Todo

### 依赖

- [ ] 根目录 `package.json` 中已声明 `@inquirer/prompts` devDependency（v0.1.0 阶段完成）

### scripts/create.mjs

- [ ] 使用 `@inquirer/prompts` 的 `checkbox` 提供多选交互界面
  ```
  ? What would you like to create?
  ❯ ◉ frontend  (Next.js 15)
    ◉ backend   (NestJS 11)
  ```

- [ ] 定义模板映射表（目标目录名 → 模板目录名 + 描述）

- [ ] `copyTemplate(templateName, targetName)` 函数
  - 目标目录已存在则跳过并提示
  - 使用 `fs.cpSync` 递归复制
  - 复制完成后修改目标 `package.json` 的 `name` 字段为目标目录名（frontend / backend）
  - 返回是否成功创建

- [ ] 用户未选择任何项时，直接退出

- [ ] 至少有一个项目被创建时，执行 `pnpm install`（`execSync`，继承 stdio）

- [ ] 安装完成后输出 next steps 提示
  - frontend 被创建：`pnpm --filter frontend dev   # Next.js on :3000`
  - backend 被创建：`pnpm --filter backend dev    # NestJS on :3001`
