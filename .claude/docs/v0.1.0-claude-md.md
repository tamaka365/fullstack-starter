# v0.1.0 — CLAUDE.md 设计

## 目标

设计 `CLAUDE.md` 与 `CLAUDE.main.md` 的内容结构，明确 Claude Code 在本项目中的工作上下文与行为约束。

---

## Todo

- [x] 两个版本均添加「项目描述」节：以 `README.md` 为准；README 不存在则主动创建；README 缺少项目描述则向用户确认后写入
- [x] `CLAUDE.md`（dev 版）—— 聚焦分支策略、hook 机制、dev 专用内容说明
- [x] `CLAUDE.main.md`（模板版）—— 聚焦架构、常用命令、添加新包的步骤
