# v0.1.0 — Monorepo 基础搭建

## 目标

初始化 pnpm monorepo 工作空间，建立根目录配置，创建三个共享包。

---

## Todo

### 根目录配置

- [ ] `package.json` — 配置 workspace 级别脚本（create、build:packages、dev、typecheck）；添加 `@inquirer/prompts`、`typescript` devDependencies；设置 engines（node >=20，pnpm >=9）
- [ ] `pnpm-workspace.yaml` — 声明工作空间范围：`packages/*`、`templates/*`、`frontend`、`backend`
- [ ] `tsconfig.base.json` — 仅供 `packages/` 下各包继承，不被 templates 使用；配置 target ES2022、strict、moduleResolution bundler、jsx react-jsx 等
- [ ] `.gitignore` — 忽略 node_modules、dist、.next、build、*.local、.DS_Store

### packages/ui

包名：`@starter/ui`

- [ ] `package.json` — 声明 `@vanilla-extract/css` 为 dependency；`exports` 指向 `./src/index.ts`（transpilePackages 方式，不预编译）；react >=18 为 peerDependency
- [ ] `tsconfig.json` — 继承 `../../tsconfig.base.json`；noEmit true（仅用于 typecheck）
- [ ] `src/index.ts` — 导出所有组件和类型
- [ ] `src/components/Button/button.css.ts` — 用 vanilla-extract `style` / `styleVariants` 定义 base、variants（primary / secondary / ghost）、sizes（sm / md / lg）
- [ ] `src/components/Button/index.tsx` — Button 组件，接受 variant、size、className、children 及原生 button 属性

### packages/hooks

包名：`@starter/hooks`

- [ ] `package.json` — 无额外 dependencies；`exports` 指向 `./src/index.ts`；react >=18 为 peerDependency
- [ ] `tsconfig.json` — 继承 `../../tsconfig.base.json`；noEmit true
- [ ] `src/index.ts` — 导出所有 hook
- [ ] `src/useLocalStorage.ts` — 泛型 hook，读写 localStorage，SSR 安全（window 判断）
- [ ] `src/useDebounce.ts` — 泛型 hook，延迟更新值

### packages/utils

包名：`@starter/utils`

- [ ] `package.json` — 无额外 dependencies；`exports` 指向 `./src/index.ts`
- [ ] `tsconfig.json` — 继承 `../../tsconfig.base.json`；noEmit true
- [ ] `src/index.ts` — 导出 `cn`（classname 合并工具函数）
